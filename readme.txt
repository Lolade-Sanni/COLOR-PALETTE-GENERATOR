A simple desktop application built with CustomTkinter that generates random color palettes and allows you to save them as images.  

Features
- Generate random palettes of up to 4 colors.
- Display hexadecimal codes for each color.
- Save generated palettes as PNG images.